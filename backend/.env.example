OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o-mini
PORT=4000
LATEX_COMPILER=tectonic # or pdflatex; must be installed on the server
ALLOW_REMOTE_COMPILER=true # fallback to hosted compiler when local engine is unavailable
COMPILE_TIMEOUT_MS=60000
REMOTE_TIMEOUT_MS=60000
# the api calls that we are going to be using 
# available options: chat/completions, embeddings
